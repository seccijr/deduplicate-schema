{
    "name": "deduplicate-schema",
    "version": "1.0.0",
    "description": "A library to deduplicate objects in a JSON schema",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc && npm run copy-test-files",
        "copy-test-files": "copyfiles -u 1 tests/files/* dist/tests",
        "test": "npm run build && jest"
    },
    "dependencies": {
        "lodash": "^4.17.21"
    },
    "devDependencies": {
        "@types/jest": "^27.5.2",
        "@types/lodash": "^4.14.198",
        "@types/node": "^20.6.0",
        "copyfiles": "^2.4.1",
        "jest": "^27.0.6",
        "ts-jest": "^27.1.5",
        "typescript": "^4.9.5"
    }
}
